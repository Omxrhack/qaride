import 'package:flutter/material.dart';

class MapaspagesScreen extends StatelessWidget {
  const MapaspagesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mapas Pages'),
    );
  }
}
