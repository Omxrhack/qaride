import 'package:flutter/material.dart';

class DireccionespagesScreen extends StatelessWidget {
  const DireccionespagesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Direcciones Pages'),
    );
  }
}
